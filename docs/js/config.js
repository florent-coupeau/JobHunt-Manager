/* Configuration Supabase — à remplir une seule fois (voir INSTALLATION.md, étape 3).
   La clé "anon" est PUBLIQUE par conception : la sécurité vient des règles RLS
   côté base (chaque utilisateur ne peut lire/écrire que ses propres lignes). */

export const SUPABASE_URL = "";      // ex. "https://abcdefghij.supabase.co"
export const SUPABASE_ANON_KEY = ""; // la clé "anon / public" du projet
