# Copie ce fichier vers « .env » (dans ce même dossier compagnon/) et remplis-le.
# ⚠️ Le fichier .env est ignoré par git (il ne sera JAMAIS publié) — vérifie quand
#    même de ne jamais commiter tes identifiants.

# Tes identifiants du SITE (les mêmes que sur la page de connexion) :
EMAIL=ton.email@exemple.fr
MOT_DE_PASSE=ton-mot-de-passe
